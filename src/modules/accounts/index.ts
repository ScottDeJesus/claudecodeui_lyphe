// The accounts module's whole public surface: the sidebar renders the row, and everything
// else — the panel, the meters, the two Descent hooks — is this module's own business.
export { AccountFooterRow } from '@/modules/accounts/AccountFooterRow';
